<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="custom_style">
        <item name="android:textSize">@dimen/def_text_size</item>
        <item name="android:layout_marginTop">@dimen/def_margin</item>
        <item name="android:layout_marginEnd">@dimen/def_margin</item>
        <item name="android:layout_marginBottom">@dimen/def_margin</item>
    </style>

</resources>
